# 后续改进：整个 App 字体大小

状态：按用户2026-09-20最新指示延期，本次v2.2.0不实现，优先发布。

需求：增加全局字体大小设置，覆盖按钮、设置文字、播放字幕和字幕回看记录。调整立即生效，重启后保留，提供恢复默认。系统菜单栏和系统对话框的可调整范围需按macOS能力核实。

实施时检查较大字号在最小窗口下是否裁切，设置和回看仍可滚动，字幕不挤出画面；不通过缩小字体来规避问题。建议倍率85–140%，默认100%，待下一版本确定。玻璃背景透明度与字幕背景透明度独立。

验证保持最小边界：相关编译、字号两端与恢复默认的无媒体界面检查；不重复无关测试。
